import { FixedNavBar } from "./FixedNavBar";
import {shallow} from "enzyme";

let wrapper;

beforeEach(() => {
    wrapper = shallow(<FixedNavBar/>)
})

describe('<FixedNavBar/> render', () => {
    test('<FixedNavBar/> snapshot',  () => {
        
    })
})
